/*class Foo(var bar: int, var baz: String) {
    function fooBar(): String {
        println(bar);
        println(baz);
        return baz;
    }
} */

function farewell(y: int) {
    print("Goodbye, ");
    println(y);
}

function hello(a: int) {
    print("Hello, ");
    println(a);
} // 实际上 Type annotation 还没用

function main() {
    var year = 2025;
    farewell(year);
    year++;
    hello(0x7EA);
}
