/*
class Foo(var bar: int, var baz: String) {
    function fooBar(): String {
        println(bar);
        println(baz);
        return baz;
    }
}
*/

var r: int = 0;

function circleArea(r: int): float {
    val area = 3.14 * r * r;
    return area;
}

function main() {
    r = 4;
    println("r=" + r + ", area=" + circleArea(r));
    r = 5;
    println("r=" + r + ", area=" + circleArea(r));
}
