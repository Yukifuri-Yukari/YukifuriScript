// Recursive.yuki

function fib(a: int): int {
    if (a <= 2)
        return 1;
    return fib(a - 1) + fib(a - 2);
}

function main() {
    println("Calculating fib(1) to fib(30): ");
    for (var i=1; i<31; i=i+1) {
        println("fib(" + i + "): " + fib(i));
    }
}
